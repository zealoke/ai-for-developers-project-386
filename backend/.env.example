# HTTP port for the API server. Default: 3000.
PORT=3000

# Disable seed data on startup. Any value other than "false" keeps seeding on.
# Seed fills 3 event types + a weekday 09:00-18:00 schedule in Europe/Moscow.
# Bookings are never seeded.
SEED=true